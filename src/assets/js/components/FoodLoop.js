import React from 'react'
import PropTypes from 'prop-types'

function FoodLoop({ food, id }) {

}

FoodLoop.propTypes = {
    foog: PropTypes.object.isRequired,
    id: PropTypes.number.isRequired
}

export default FoodLoop